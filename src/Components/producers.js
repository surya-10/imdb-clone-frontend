import { useParams } from "react-router-dom";
import Base, { myContext } from "./base";
import { useContext } from "react";

function Producers({name}){
    return (
        <div className="producers-div">
            <Base>
            </Base>
        </div>
    )
}
export default Producers;